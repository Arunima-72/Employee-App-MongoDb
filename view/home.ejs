<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/home.css">
    <link rel="icon" href="/images/groupfavicon.svg" type="image/x-icon">

</head>
<body>
  <!-- Navbar Start -->
<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid">
    <img src="/images/group.png" alt="Employer App Logo" style="height: 80px;">
    <a class="navbar-brand ms-2" href="#" style="font-family: 'Times New Roman', Times, serif;font-size: x-large;">Employee App</a>

    <!-- Toggler button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible menu -->
    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% for(let i = 0; i < nav.length; i++) { %>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="<%= nav[i].link %>"><%= nav[i].name %></a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
<!-- Navbar End -->


<div class="container mt-4">
  <div class="mb-4">
    <h4 class="fw-bold">Employee Directory</h4>
    <p class="text-muted mb-0">Manage employee details including designation, location, and salary. Keep your organization structured and up-to-date.</p>
  </div>

    <div class="container mt-4">
      <table class="table table-bordered pastel-table">
        <thead>
          <tr>
            <th>Id No</th>
            <th>Name</th>
            <th>Designation</th>
            <th>Location</th>
            <th>Salary</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% for(let i = 0; i < data.length; i++) { %>
            <tr>
              <td><%= i + 1 %></td>
              <td><%= data[i].employeeName %></td>
              <td><%= data[i].employeeDesignation %></td>
              <td><%= data[i].employeeLocation %></td>
              <td><%= data[i].employeeSalary %></td>
              <td>
                <a class="btn btn-success" href="/basic/updateform/<%=data[i]._id%>">Update</a>
                <a class="btn btn-danger" href="/basic/delete/<%=data[i]._id%>">Delete</a> 
                <!-- <a class="btn btn-primary btn-sm" href="/basic/updateform/<%=data[i]._id%>">Update</a>
                <a class="btn btn-danger btn-sm" href="/basic/delete/<%=data[i]._id%>">Delete</a> -->
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
    
    <% if (success) { %>
      <div class="alert alert-success alert-dismissible fade show mt-3 mx-3" role="alert">
        <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>
    
    <% if (error) { %>
      <div class="alert alert-danger alert-dismissible fade show mt-3 mx-3" role="alert">
        <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% } %>
    

   
       
</body>
</html> 